apiVersion: example.org/v1
kind: Xs3Claim  
metadata:
  name: my-xs3-claim
spec: 
  name: "crossplane-bucket-1"
  region: "us-east-1"
  namespace: "demo"
  compositionRef:
    name: s3bucket-crossplane